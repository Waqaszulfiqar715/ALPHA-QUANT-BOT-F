# Vercel Environment Variables Configuration
# Optional: Set this in Vercel Dashboard -> Project Settings -> Environment Variables
# If not set, vercel.json will automatically proxy /api requests to your deployed backend.
VITE_BACKEND_URL=https://alpha-quant-bot-b.onrender.com
